{
  "name": "ts-utils-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  },
  "scripts": {
    "build": "npm run build --workspaces"
  }
}
